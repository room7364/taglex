<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>Taglex: Home page</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" 
          rel="stylesheet" 
          integrity="sha384-GLhlTQ8iRABdZLl6O3oVMWSktQOp6b7In1Zl3/Jr59b6EGGoI1aFkw7cmDA6j6gD" 
          crossorigin="anonymous"
    >
  </head>

  <body>
    <form class="container" action="http://localhost:8080/search.rhtml" method="POST">
      <input name="word" id="word" />
      <button>Search</button>
    </form>

    <% if servlet_request.request_method == "POST" then %>
      <% stems = Model.stems(servlet_request.query["word"]) %>
      <p><%= stems.to_s %></p>
      <% stems.each do |word| %>
        <% results = Model.find word %>
        <% if results.size != 0 then %>
          <h4><%= word %></h4>
          <% results.each do |result| %>
            <div class="container border border-3 border-dark">
              <div class="row">
                <div class="col border border-1">
                  <h5>Face</h5>
                      <h6>Questions</h6>
                          <ul>
                            <% result["face"]["questions"].each do |line| %>
                              <li><%= line %></li>
                            <% end %>
                          </ul>
                      <h6>Examples</h6>
                          <ul>
                            <% result["face"]["examples"].each do |line| %>
                              <li><%= line %></li>
                            <% end %>
                          </ul>
                </div>
                <div class="col border border-1">
                  <h5>Back</h5>
                      <h6>Answers</h6>
                          <ul>
                            <% result["back"]["answers"].each do |line| %>
                              <li><%= line %></li>
                            <% end %>
                          </ul>
                      <h6>Examples</h6>
                          <ul>
                            <% result["back"]["examples"].each do |line| %>
                              <li><%= line %></li>
                            <% end %>
                          </ul>
                </div>
              </div>
            </div>
          <% end %>
        <% end %>
      <% end %>
    <% end %>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/js/bootstrap.bundle.min.js" 
            integrity="sha384-w76AqPfDkMBDXo30jS1Sgez6pr3x5MlQ1ZAGC+nuZB+EYdgRZgiwxhTBTkF7CXvN" 
            crossorigin="anonymous">
    </script>
  </body>
</html>
